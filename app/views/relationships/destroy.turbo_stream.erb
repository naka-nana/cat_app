<%= turbo_stream.update "follow-button",
      partial: "users/follow_button",
      locals: { user: @user } %>

<%= turbo_stream.update "followers-count-number", "フォロワー数:#{@user.followers.count}" %>

<%= turbo_stream.update "following-count-number", "フォロー数:#{current_user.following.count}" %>