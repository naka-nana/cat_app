<!DOCTYPE html>
<html>
  <head>
    <title>CatApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <header class="header">
    <div class="header__bar row">
      <h1 class="grid-6"><a href="/">ねこのげぼく</a></h1>
      <% if user_signed_in? %>
        <div class="user_nav grid-6">
          <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete } %>
          <%= link_to "投稿する", "/", class: "post" %>
          <% if current_page?(mypage_user_path(current_user)) %>
            <%= link_to 'ねこ登録', new_cat_path, class: "post" %>
            <%= link_to 'げぼく診断', "/", class: "post" %> <!-- ここで「げぼく診断」に切り替え -->
          <% else %>
            <%= link_to 'マイページ', mypage_user_path(current_user), class: "post" %>
          <% end %>
        </div>
      <% else %>
        <div class="user_nav grid-6">
          <%= link_to "ログイン", new_user_session_path, class: "post" %>
          <%= link_to "新規登録", new_user_registration_path, class: "post" %>
        </div>
      <% end %>
    </div>
  </header>
    <%= yield %>
    <footer>
      <p>
        Copyright CatApp 2025.
      </p>
    </footer>
  </body>
</html>
